import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Que Comemos Hoy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
